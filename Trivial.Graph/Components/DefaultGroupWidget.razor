@code {

    [Parameter]
    public GroupModel Group { get; set; } = null!;

}

<GroupNodes Group="Group"/>

@foreach (var t_Port in Group.Ports)
{
    <PortRenderer @key="t_Port" Port="t_Port" Class="group-port"></PortRenderer>
}